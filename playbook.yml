---
-
  hosts:
    - localhost
    - all
  roles:
    - { role: centos, when: ansible_distribution == 'CentOS' }
    - network
    - database
    - minio
    - redis
    - session-service
    - ws-service
  vars_files:
    - vars.yml
