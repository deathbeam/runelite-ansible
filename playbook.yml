---
-
  hosts:
    - localhost
    - all
  roles:
    - { role: centos, when: ansible_distribution == 'CentOS' }
    - network
    - database
    - minio
    - redis
    - cache-updater
    - { role: tomcat, when: tomcat.deploy }
    - { role: http-service }
  vars_files:
    - vars.yml
