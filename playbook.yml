---
-
  hosts:
    - localhost
    - all
  roles:
    - { role: centos, when: ansible_distribution == 'CentOS' }
    - network
    - database
    - minio
    - redis
    - cache-updater
    - { role: tomcat, when: tomcat.enabled }
    - { role: http-service, when: not tomcat.enabled }
  vars_files:
    - vars.yml
