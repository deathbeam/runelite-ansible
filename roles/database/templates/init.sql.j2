create user if not exists '{{ database.user }}'@'%' identified by '{{ database.password }}';

{% for resource in resources %}
create database if not exists `{{ resource.database }}`;
grant all on `{{ resource.database }}`.* to '{{ database.user }}'@'%' identified by '{{ database.password }}' with grant option;
{% endfor %}
flush privileges;

use `runelite-tracker`;
create function level_for_xp returns integer soname 'libxp.so';
